# Copy this file to .env and fill in the actual secret values.
# NEVER commit .env to source control.

# Database connection string
ConnectionStrings__DefaultConnection=User Id=<CloudSAcbd8209c>;Password=<Ashu@123>;Server=<locaalhost.database.windows.net>;Database=<appdb>;Trusted_Connection=False;Persist Security Info=False;Encrypt=True;

# JWT signing key (must be at least 32 characters)
JWT__Key=<ayeayecaptainwethrowedtheanchorshelloworld>

